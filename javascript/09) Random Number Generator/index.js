

let deRandom = Math.random(); // default it will give between 0 and 1

console.log(deRandom);

let random = Math.random() * 10 + 1; // between 1 and 10
console.log(Math.trunc(random)); // printing without the decimal point